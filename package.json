{
  "name": "customer-service-app",
  "version": "1.0.0",
  "description": "A demo customer service app with OpenAI tool calling",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/src/index.js",
    "dev": "ts-node src/index.ts",
    "debug": "npm run build && node --inspect-brk dist/src/index.js"
  },
  "dependencies": {
    "openai": "^4.28.0",
    "dotenv": "^16.3.1",
    "readline": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
